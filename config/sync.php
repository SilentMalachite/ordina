<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 同期サーバー設定
    |--------------------------------------------------------------------------
    |
    | オフライン機能で使用する中央サーバーのエンドポイントとAPIトークンを
    | 設定します。NativePHP環境では、これらの設定は環境変数または設定
    | ファイルから読み込まれます。
    |
    */

    'server_url' => env('SYNC_SERVER_URL', 'https://api.ordina.example.com'),
    
    'api_token' => env('SYNC_API_TOKEN'),
    
    /*
    |--------------------------------------------------------------------------
    | 同期間隔
    |--------------------------------------------------------------------------
    |
    | 自動同期を実行する間隔（秒単位）を設定します。
    |
    */
    
    'auto_sync_interval' => env('SYNC_INTERVAL', 300), // 5分
    
    /*
    |--------------------------------------------------------------------------
    | 同期タイムアウト
    |--------------------------------------------------------------------------
    |
    | 同期処理のタイムアウト時間（秒単位）を設定します。
    |
    */
    
    'timeout' => env('SYNC_TIMEOUT', 30),
    
    /*
    |--------------------------------------------------------------------------
    | オフラインモード
    |--------------------------------------------------------------------------
    |
    | 強制的にオフラインモードで動作させる場合はtrueに設定します。
    | 開発やテスト時に便利です。
    |
    */
    
    'force_offline' => env('SYNC_FORCE_OFFLINE', false),
];